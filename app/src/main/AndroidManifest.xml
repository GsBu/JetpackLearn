<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jobs.android.jetpacklearn">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackLearn">
        <activity
            android:name="com.jobs.android.jetpacklearn.notification.NotificationActivity"
            android:exported="false" />
        <activity
            android:name="com.jobs.android.jetpacklearn.taskrecord.TaskRecordActivity"
            android:exported="false"
            android:taskAffinity=":aaaaa" />
        <activity
            android:name="com.jobs.android.jetpacklearn.server.StudentActivity"
            android:exported="false" />
        <activity android:name=".leak.LeakActivity" />
        <activity android:name=".viewmodel.ViewModelActivity" />
        <activity android:name=".livedata.LiveDataTestActivity" />
        <activity android:name=".databinding.DataBindingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.jobs.android.jetpacklearn.server.StudentService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" />
    </application>

</manifest>